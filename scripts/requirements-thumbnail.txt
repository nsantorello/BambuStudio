# Requirements for headless_thumbnail.py
# Install with: pip install -r requirements-thumbnail.txt

# Core dependencies (required)
numpy>=1.20.0
trimesh>=3.10.0
Pillow>=8.0.0
scipy>=1.7.0

# For 3MF file support (recommended)
networkx>=2.6.0
lxml>=4.6.0

# For higher quality rendering with OSMesa (optional, recommended)
# These enable hardware-quality rendering in headless environments
# Note: May require system dependencies (see below)
# pyrender>=0.1.45
# PyOpenGL>=3.1.5
# pyglet<2

# System dependencies for OSMesa (install separately):
# Ubuntu/Debian: sudo apt-get install libosmesa6-dev mesa-utils
# Fedora/RHEL:   sudo dnf install mesa-libOSMesa-devel
# Arch Linux:    sudo pacman -S mesa

# For STEP file support (optional)
# Choose ONE of the following:
#
# Option 1: cadquery (easier to install)
# cadquery>=2.0.0
#
# Option 2: OCP (more lightweight)
# OCP>=7.7.0
